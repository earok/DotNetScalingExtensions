# DotNetScalingExtensions
Just some simple pixel art scaling extension methods for DotNet (system.drawing)

They can be chained like so:

var newBitmap = Bitmap.Scale2X().Scale2X().Scale2X()

To create a Scale8X bitmap.
